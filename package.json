{
    "name": "@hmhealey/mtgtools",
    "packageManager": "yarn@3.3.0",
    "version": "0.1.0",
    "license": "ISC",
    "private": true,
    "scripts": {
        "clean": "rm -rf .yarn/cache .yarn/unplugged .yarn/install-state.gz && yarn workspaces foreach run clean",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "workspaces": [
        "app",
        "packages/f2f",
        "packages/scryfall"
    ],
    "devDependencies": {
        "@types/jest": "^29.2.3",
        "typescript": "^4.9.3"
    }
}
