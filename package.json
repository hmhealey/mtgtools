{
    "name": "@hmhealey/mtgtools",
    "packageManager": "yarn@3.3.0",
    "version": "0.1.0",
    "license": "ISC",
    "private": true,
    "scripts": {
        "build": "yarn workspaces foreach run build",
        "clean": "yarn workspaces foreach run clean && rm -rf .yarn/cache .yarn/unplugged .yarn/install-state.gz",
        "start": "yarn workspace app start",
        "test": "yarn workspaces foreach run test"
    },
    "workspaces": [
        "app",
        "packages/f2f",
        "packages/scryfall"
    ],
    "devDependencies": {
        "@types/jest": "^29.2.3",
        "typescript": "^4.9.3"
    }
}
