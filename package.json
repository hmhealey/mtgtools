{
  "name": "@hmhealey/mtgtools",
  "packageManager": "yarn@3.3.0",
  "version": "0.1.0",
  "license": "ISC",
  "private": true,
  "scripts": {
    "clean": "rm -r .yarn/cache .yarn/unplugged .yarn/install-state.gz",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^4.9.3"
  }
}
