{
    "name": "@hmhealey/mtgtools",
    "version": "0.1.0",
    "license": "ISC",
    "private": true,
    "scripts": {
        "build": "npm --workspaces --if-present run build",
        "clean": "npm --workspaces --if-present run clean && rm -rf node_modules",
        "start": "npm --workspaces --if-present start",
        "test": "npm --workspaces --if-present test"
    },
    "workspaces": [
        "app",
        "packages/f2f",
        "packages/scryfall"
    ],
    "devDependencies": {
        "@types/jest": "^29.2.3",
        "typescript": "^4.9.3"
    }
}
